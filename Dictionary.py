
#Note: Data stored in the form of Key-Value pairs.
#     Mutable in nature(But we can change only values not the keys)

# Dictionaries can allow only duplicate values but not the duplicate keys(unique)


d = {
    1:10,
    2:1,
    "Hello":300            #Dictionary Creation
}


print(d["Hello"])           #Dictionary Accessing(1 is the key)